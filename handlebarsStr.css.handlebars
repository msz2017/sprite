{{#sprites}}
.icon-{{name}} {
  background-image: image_url('sprites/{{{escaped_image}}}');
  background-position: {{half offset_x}} {{half offset_y}};
  background-size: {{half total_width}} {{half total_height}};
  width: {{half width}};
  height: {{half height}};
}
{{/sprites}}